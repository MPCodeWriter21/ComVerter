# Stream to stream

- STATUS: OPEN
- PRIORITY: 60
- TAGS: file, feature, enhancement

Add a function to the C code that accepts a stream/file to read from and directly writes the output to a stream/file.

The API of the module must be implemented toughtfully and must provide great utility.
