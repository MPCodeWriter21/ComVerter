# Add themes to markdown to HTML system

- STATUS: OPEN
- PRIORITY: 60
- TAGS: html, theme, feature, markdown, enhancement

We should accept themes that will apply different CSS to the output HTML and make it look better. There should be at least three pre-existing options for themes to be available NONE(no theme), LIGHT(GitHub like light theme), and DARK(GitHub like dark theme)

This is a good addition to Task(20260622-140002) and may be good if we add it in a way that it's available within code as well (this is supposed to be a library after all).
